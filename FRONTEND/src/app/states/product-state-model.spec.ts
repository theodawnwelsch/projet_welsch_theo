import { ProductStateModel } from './product-state-model';

describe('ProductStateModel', () => {
  it('should create an instance', () => {
    expect(new ProductStateModel()).toBeTruthy();
  });
});
